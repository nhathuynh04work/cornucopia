function CourseSettings({ course }) {
	return (
		<div>
			<h2 className="text-xl font-semibold">Course Settings</h2>
			<p className="mt-4">
				Placeholders for settings (e.g., publish course, delete course).
			</p>
		</div>
	);
}

export default CourseSettings;
